//celsius temperature to be changed
var celsiusTemp = 37;
//storing the equation in a variable converter
var converter = (celsiusTemp * 9/5 + 32);
let result = converter;
//logging the converted temp to the console
console.log(result);
